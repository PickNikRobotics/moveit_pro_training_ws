<root>
    <TreeNodesModel>
        <Action ID="GetAprilTagDetectionPose">
            <metadata subcategory="Perception"/>
            <description>
                <p>
                    Gets an AprilTag detection pose, if any, given a specific ID to filter by.
                </p>
            </description>
            <input_port name="apriltag_id" default="0">ID of AprilTag to look for.</input_port>
            <input_port name="apriltag_size" default="0.05">Size AprilTag to look for, in meters.</input_port>
            <input_port name="camera_info" default="{camera_info}">Camera information ROS message.</input_port>
            <input_port name="image" default="{image}">Image ROS message.</input_port>
            <output_port name="detection_pose" default="{detection_pose}">Pose of the detected AprilTag.</output_port>
        </Action>
        <Action ID="SetupMTCPickAprilTag">
            <metadata subcategory="MTC"/>
            <description>
                <p>
                    Given an existing MTC Task object and a target grasp pose, appends MTC stages to describe a motion plan to approach, grasp and lift an AprilTag annotated object at that pose.
                </p>
            </description>
            <input_port name="parameters" default="{parameters}">Behavior parameters stored in a common configuration file for the objective.</input_port>
            <input_port name="grasp_pose" default="{grasp_pose}">End-effector grasping pose.</input_port>
            <inout_port name="task" default="{mtc_task}">MoveIt Task Constructor task.</inout_port>
        </Action>
    </TreeNodesModel>
</root>
